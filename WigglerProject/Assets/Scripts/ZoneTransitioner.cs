using System;
using UnityEditor;
using UnityEngine;
using UnityEngine.SceneManagement;

public class ZoneTransitioner : MonoBehaviour
{
   /*public SceneAsset sceneToLoad;
   public SceneAsset sceneToUnload;

   private bool hasHitTrigger;
   private void OnTriggerEnter(Collider other)
   {
      if (other.TryGetComponent(out PlayerController playerController))
      {
         if (hasHitTrigger)
         {
            
            SceneManager.LoadSceneAsync(sceneToLoad.name, LoadSceneMode.Additive);
            SceneManager.UnloadSceneAsync(sceneToUnload.name);
            hasHitTrigger = false;
         }

         else
         {
            
            SceneManager.LoadSceneAsync(sceneToUnload.name, LoadSceneMode.Additive);
            SceneManager.UnloadSceneAsync(sceneToLoad.name);
            hasHitTrigger = true;
         }

      }
   }*/
}
